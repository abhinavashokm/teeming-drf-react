
export const ROUTE_PATHS = {
    ROOT: '/',
    WORKSPACES: '/workspaces',
    WORKSPACE: (slug) => `/w/${slug}`,
    CREATE_WORKSPACE: '/create-workspace',
    LOGIN: '/auth/login',
    SIGNUP: '/auth/signup',
}